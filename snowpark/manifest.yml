manifest_version: 1

version:
  name: V1
  label: "Dekart V1"
  comment: "Initial version of the Dekart package"


artifacts:
  readme: readme.md
  setup_script: setup.sql
  container_services:
    images:
      - /dekart_app/napp/img_repo/dekart_image

privileges:
 - BIND SERVICE ENDPOINT:
      description: "Enables application to expose service endpoints"


configuration:
  log_level: debug
  trace_level: always
