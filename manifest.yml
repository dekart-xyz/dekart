version:
  name: V1
  label: "First Version"

artifacts:
  setup_script: setup.sql
  container_services:
    images:
    - /dekart_image_database/dekart_image_schema/dekart_image_repo/dekart-snowpark

privileges:
- BIND SERVICE ENDPOINT:
    description: "A service that can respond to requests from public endpoints."